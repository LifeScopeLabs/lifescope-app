query dataRemovalConfirmationCheck($confirmation_code: String!) {
    dataRemovalConfirmationCheck(confirmation_code: $confirmation_code) {
        noRecord,
        providerName,
        success,
    }
}
