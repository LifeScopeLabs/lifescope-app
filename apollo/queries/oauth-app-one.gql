query oauthAppOne($id: String!) {
    oauthAppOne(filter: { id: $id }) {
        id,
		client_id,
		description,
		homepage_url,
		name,
		privacy_policy_url,
        provider_id,
        provider_id_string,
		redirect_uris
    }
}
