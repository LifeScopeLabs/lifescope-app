mutation initializeConnection($provider_id_string: String!, $permissions: JSON, $name: String, $app_session: Boolean) {
    initializeConnection(provider_id_string: $provider_id_string, permissions: $permissions, name: $name, app_session: $app_session) {
        redirectUrl
    }
}