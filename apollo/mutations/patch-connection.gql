mutation patchConnection($id: String!, $enabled: Boolean, $permissions: JSON, $name: String) {
    connectionPatch(id: $id, enabled: $enabled, permissions: $permissions, name: $name) {
        connection,
        reauthorize
    }
}