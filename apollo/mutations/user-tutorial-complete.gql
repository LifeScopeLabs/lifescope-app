mutation userTutorialComplete($tutorial: String!) {
     userTutorialComplete(tutorial: $tutorial) {
        id,
        tutorials {
            connections,
            explorer,
            home
        }
    }
}
