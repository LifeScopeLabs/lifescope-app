export const state = function () {
    return {
        hoverColor: '#2ac1de',
        activeColor: '#f93',
     };
};

const styleModule = {
    namespaced: true,
    state,
}

export default styleModule;
